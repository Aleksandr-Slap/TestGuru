<% if test.ready == true %> 
  <tr>
    <td><%= test.title  %></td>
    <td><%= test_level(test) %></td> 
    <td><%= test.questions.count %></td>
    <td><%= test.timer %></td>
    
    <td><%= button_to t('.button_start'), start_test_path(test) %></td>

  </tr>
<% end %>  
